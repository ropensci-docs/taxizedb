# database path

database path

## Usage

``` r
db_path(db)
```

## Arguments

- db:

  (character) db name. one of: itis, tpl, col, gbif, ncbi, wikidata,
  wfo. required

## Value

Path to the local database.
